var config = {}
config.mongodb = {}
config.host = {}
config.websocket = {}

config.mongodb.url = "mongodb://localhost/"
config.mongodb.database = "254scouting"

config.host.ip = "0.0.0.0"
config.host.port = "8080"

config.websocket.port = "8082"

module.exports = config;
