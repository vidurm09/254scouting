var config = {}
config.mongodb = {}
config.host = {}
config.websocket = {}

config.mongodb.url = "mongodb://localhost/"
config.mongodb.database = "254scouting"

config.host.ip = "0.0.0.0"
config.host.port = "80"

config.websocket.port = "82"

module.exports = config;
